{
  "name": "anvila-api-regression",
  "version": "1.0.0",
  "description": "Postman/Newman regression tests for Anvila API",
  "scripts": {
    "test": "newman run collections/anvila-regression-suite.json --reporters cli,htmlextra --reporter-htmlextra-export test-report.html"
  },
  "devDependencies": {
    "newman": "^6.1.3",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}